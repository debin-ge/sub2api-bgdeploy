# Design documents

This directory contains the original requirements, design alternatives,
implementation plan, and application background-task audit that accompanied
the deployment tool.

Some documents are historical records and intentionally retain references to
the original application repository, directory layout, and superseded script
names. For current installation and command behavior, use the repository-level
`README.md` and `./bgdeploy --help`.
